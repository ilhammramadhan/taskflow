# Force npm to install devDependencies even when NODE_ENV=production.
# Required because TypeScript build needs @types/* packages (in devDeps)
# but Render sets NODE_ENV=production which makes npm skip devDeps by default.
production=false
